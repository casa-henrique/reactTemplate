import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  align-items: center;
  
  gap: 1rem;
  padding: 0.8rem 3rem;

  border: 2px solid #F1C40F;
  border-radius: 8px;
  color: #F1C40F;
  font-weight: bold;
  background-color: rgba(242, 231, 127, 0.25);

  > svg {
    width: 1.5rem;
    height: 1.5rem;
    color: #F1C40F;
  }
  `